{
  "name": "温度监控平台",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:local": "PATH=$PWD/node_modules/.bin:$PATH vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run test:api-client",
    "test:api-client": "rm -rf .tmp/test-build && tsc -p tsconfig.test.json && node --test .tmp/test-build/tests/*.test.js"
  },
  "dependencies": {
    "@google/genai": "^1.31.0",
    "lucide-react": "^0.554.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
